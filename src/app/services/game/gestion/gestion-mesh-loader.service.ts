import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class GestionMeshLoaderService {
  constructor() { }

/*
  public load1stQG(posX: number, posY: number, scene: any) {
    BABYLON.SceneLoader.ImportMesh("", "./", "duck.gltf", scene, function (meshes, particleSystems, skeletons) {
    // do something with the meshes and skeletons
    // particleSystems are always null for glTF assets
    });
  }
*/
}
